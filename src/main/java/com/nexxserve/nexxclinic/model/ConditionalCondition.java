package com.nexxserve.nexxclinic.model;

public enum ConditionalCondition {
    equals,
    not_equals,
    contains,
    not_contains,
    greater_than,
    less_than,
    is_empty,
    is_not_empty,
    hasItem
}
